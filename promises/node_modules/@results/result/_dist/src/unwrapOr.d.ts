import { Result } from "@jsr/results__types";
/**
 * Unwraps a result, returning a default value if it is an error
 * @param result
 * @param defaultValue a value to return if the result is an error
 * @returns `T` where `Result<T, E>` is the type of the result
 */ export declare function unwrapOr<T, E>(result: Result<T, E>, defaultValue: NoInfer<T>): T;
//# sourceMappingURL=unwrapOr.d.ts.map